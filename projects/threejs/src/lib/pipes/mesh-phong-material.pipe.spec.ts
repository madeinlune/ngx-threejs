import { MeshPhongMaterialPipe } from './mesh-phong-material.pipe';

describe('MeshPhongMaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new MeshPhongMaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
