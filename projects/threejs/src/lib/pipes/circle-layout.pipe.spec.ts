import { CircleLayoutPipe } from './circle-layout.pipe';

describe('CircleLayoutPipe', () => {
  it('create an instance', () => {
    const pipe = new CircleLayoutPipe();
    expect(pipe).toBeTruthy();
  });
});
