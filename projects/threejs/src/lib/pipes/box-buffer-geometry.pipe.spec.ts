import { BoxBufferGeometryPipe } from './box-buffer-geometry.pipe';

describe('BoxBufferGeometryPipe', () => {
  it('create an instance', () => {
    const pipe = new BoxBufferGeometryPipe();
    expect(pipe).toBeTruthy();
  });
});
