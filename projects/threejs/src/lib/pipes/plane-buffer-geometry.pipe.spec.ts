import { PlaneBufferGeometryPipe } from './plane-buffer-geometry.pipe';

describe('PlaneBufferGeometryPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaneBufferGeometryPipe();
    expect(pipe).toBeTruthy();
  });
});
