import { Position3dDirective } from './position-3d.directive';

describe('Position3dDirective', () => {
  it('should create an instance', () => {
    const directive = new Position3dDirective();
    expect(directive).toBeTruthy();
  });
});
