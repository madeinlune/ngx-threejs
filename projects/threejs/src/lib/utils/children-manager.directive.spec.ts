import { ChildrenManagerDirective } from './children-manager.directive';

describe('ChildrenManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildrenManagerDirective();
    expect(directive).toBeTruthy();
  });
});
